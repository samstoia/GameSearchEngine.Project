@using GameSearchEngine.Models;

@{
    Layout = "_Layout";
}
<div class="page-body">
    <h1>List of games by release date:</h1>
    <div class="scrollable">
        <ul>
        @foreach (var game in Model)
        {
            <li><a href='/releasedate/@game.GetId()'>@game.GetYear() - @game.GetTitle() </a></li>
        }
        </ul>
    </div>

</div>