@using GameSearchEngine.Models;

@{
    Layout = "_Layout";
}

<div class="page-body">
    <h1></h1>
    <ul>
        @foreach (var game in Model)
        {
            <li><a href='game/@game.GetId()'>@game.GetTitle()</a></li>
        }
    </ul>
</div>
